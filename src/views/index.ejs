<!DOCTYPE html>
<html lang="pt-br">
  <%- include('./partials/head.ejs') %>
  <body>
    <%- include('./partials/navbar.ejs') %>
    <div class="content">
      <% if (users.length> 0) { %> <% users.forEach(user=> { %>

      <h3 class="title"><%= user.fisrtName %> <%= user.lastName %></h3>

      <p><%= user.email %></p>

      <% }) %> <% } else { %>
      <p>Não usuários para exibir..</p>
      <% } %>
    </div>
  </body>
</html>
